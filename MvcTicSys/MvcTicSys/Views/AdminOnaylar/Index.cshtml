@model IEnumerable<MvcTicSys.Tbl_Onaylar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
 
<p>
    @Html.ActionLink("Yeni bir onay turu olusturmak icin tiklayiniz.", "Create")
</p>
<table>
    <tr>
        <th>
            Onay
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Onay)
        </td>
        <td>
            @Html.ActionLink("Duzenle", "Edit", new { id=item.OnayID }) |
            @Html.ActionLink("Detay", "Details", new { id=item.OnayID }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.OnayID })
        </td>
    </tr>
}

</table>
